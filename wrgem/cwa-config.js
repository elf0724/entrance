// ============================================================
// 雲海預測儀 · 個人設定檔
//
// 這個檔案跟「雲海預測儀_CWA版.html」放在同一個資料夾，雙擊打開 HTML 時
// 會自動讀取這裡的設定，不需要在網頁介面上輸入任何金鑰或網址。
//
// 重要：這個檔案含有你的代理伺服器網址，等於間接連結到你的 CWA 與
// Groq 金鑰額度。分享「雲海預測儀_CWA版.html」給別人時，不要連這個檔案
// 一起給出去；只給 HTML 本體的話，對方打開會自動偵測不到這個檔案，程式
// 會自動只用 Open-Meteo 資料，不會出錯、也不會用到你的金鑰額度。
// ============================================================
window.CWA_APP_CONFIG = {
  // 你的 Cloudflare Worker 代理伺服器網址（結尾不要加斜線）
  proxyBase: 'https://cwa-proxy.elf0724.workers.dev',

  // 是否自動嘗試併入中央氣象署資料（濕度／露點／降雨機率／最近測站觀測）
  // 設成 false 可以暫時只用 Open-Meteo，不用刪掉上面的網址
  enableCwa: true,

  // 是否自動嘗試用 Groq AI 潤飾文字說明（機率與數值不受影響，AI 只改寫語氣）
  // 設成 false 會一律使用內建模板文字，不呼叫 AI
  enableGroq: true,
};
